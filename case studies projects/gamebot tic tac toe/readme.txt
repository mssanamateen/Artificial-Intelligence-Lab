 Open your terminal or command prompt and run the following commands:

pip install tensorflow
pip install numpy

import them into our Python script. Open your Python IDE or text editor and create a new Python file. Start by importing the required libraries:

import tensorflow as tf
import numpy as np
import random